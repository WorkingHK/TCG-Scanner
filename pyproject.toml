[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "tcg-grading"
version = "0.1.3"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["tcg_grading*", "desktop*"]
